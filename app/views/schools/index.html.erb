<% provide :title, "Was It Worth" %>
<h1>Pick a School</h1>
		<ul>
    <% @schools.each do |school| %>
      <li><%= link_to school.name, school.hiddenname %></li>
    <% end %>
  </ul>
<br>
<%= link_to 'New School', new_school_path, class: "btn btn-submit" %>
